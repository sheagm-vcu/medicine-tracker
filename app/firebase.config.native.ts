// Firebase configuration for React Native (native SDK)
import auth from '@react-native-firebase/auth';
import firestore from '@react-native-firebase/firestore';

// Export the native Firebase services
export { auth, firestore };

// Export default app (not needed for native SDK)
export default null;
